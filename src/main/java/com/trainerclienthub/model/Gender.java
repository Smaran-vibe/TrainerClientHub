package com.trainerclienthub.model;

/**
 * Represents the gender options available for a {@link Client}.
 * Mirrors the ENUM constraint defined in the {@code client} database table.
 */
public enum Gender {
    MALE,
    FEMALE,
    OTHER
}
